#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main(){
    string s;
    cin>>s;
    int n;
    cin>>n;
    int a[n];
    for (int i = 0; i < n; i++)
    {
        cin>>a[i];
    }
    int sum[s.length()];
    int cnt=0;
    for (int i = 0; i < s.length()/2; i++)
    {
        if(a[cnt]-1==i)
        sum[i]
    }
    
    
}